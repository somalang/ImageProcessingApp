<Window x:Class="ImageProcessing.Views.ParameterInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="150" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Prompt}"/>
        <TextBox Grid.Row="1" x:Name="InputTextBox" Margin="0,5,0,10" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Confirm" Width="60" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="60" Margin="5,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>